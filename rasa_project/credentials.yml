# Channel credentials. We only enable REST for Ex6 — the homework's
# StructuredHalf talks HTTP, not voice or Slack or anything else.

rest:

# Other channels deliberately omitted; students don't need them and
# enabling them produces warnings for each unset credential.
